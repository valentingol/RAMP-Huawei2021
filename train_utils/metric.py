import numpy as np
import sklearn

def accuracy(y, y_pred):
    """Compute accuracy from sklearn

    Parameters
    ----------
    y : np.array
        true labels
    y_pred : np.array
        predicted labels

    Returns
    -------
    acc : float
        accuracy using sklearn library
    """
    # $$ implement accuracy $$
    acc = None
    return acc

def area_ROC(y, y_pred):
    # $$ implement area_ROC $$
    auc = None
    return auc

def av_precision(y, y_pred):
    # $$ implement av_precision $$
    ap = None
    return ap

def prec_recall(y, y_pred):
    # $$ implement prec_recall $$
    pr = None
    return pr
